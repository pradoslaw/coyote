{% extends 'layout.twig' %}
{% block title %}Rejestracja{{ parent() }}{% endblock %}

{% import 'components/forms.twig' as forms %}

{% block container %}
    <div class="row" style="margin-top: 25px">

        <div class="col-xs-10 col-xs-offset-1">
            <div id="box-register" class="col-sm-5">
                <h1>Darmowa rejestracja - stwórz swój profil</h1>

                {% include 'auth/oauth.twig' %}

                {{ form_open({'method': 'POST', 'role': 'form', 'url': route('register')}) }}
                {{ forms.inline(form_text('name', input_old('name'), {'class': 'form-control'}), 'Nazwa użytkownika', errors.first('name')) }}
                {{ forms.inline(form_password('password', {'class': 'form-control'}), 'Hasło', errors.first('password')) }}
                {{ forms.inline(form_password('password_confirmation', {'class': 'form-control'}), 'Hasło (powtórnie)', errors.first('password_confirmation')) }}
                {{ forms.inline(form_text('email', input_old('email'), {'class': 'form-control'}), 'E-mail', errors.first('email'), 'Nie wysyłamy reklam. Twój e-mail nie zostanie nikomu udostępniony.') }}

                <div class="form-group">
                    <button type="submit" class="btn btn-primary" style="margin-right: 15px;" data-submit-state="Rejestracja...">
                        Utwórz konto
                    </button>
                </div>
                {{ form_close() }}
            </div>

            <div id="box-register-sidebar" class="col-sm-5 col-sm-offset-2 hidden-xs">
                <div>
                    <blockquote>Rejestracja zajmuje tylko kilka sekund i jest darmowa. Pozwoli Ci to aktywnie korzystać z naszego serwisu</blockquote>

                    <h2>Korzyści wynikające z rejestracji konta</h2>

                    <ul class="list-unstyled">
                        <li>Możliwość edycji oraz pisania artykułów</li>
                        <li>Obserwowanie wątków na forum</li>
                        <li>Śledzenie zmian w artykułach czy komentarzach</li>
                        <li>System szeregu powiadomień o aktywności na stronie</li>
                        <li>Pisanie własnego mikrobloga</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block head %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ cdn('css/auth.css') }}">
{% endblock %}

{% block body %}
    {{ parent() }}

    <script type="text/javascript">
        $(function() {
            $('#box-register').find('form').append('<input style="display: none" type="checkbox" name="human" value="1" checked="checked">');

            $('.btn-github, .btn-google, .btn-fb').click(function() {
                $(this).text('Logowanie...');
                window.location.href = $(this).data('url');
            });
        });
    </script>
{% endblock %}