{% extends 'layout.twig' %}
{% block title %}Rejestracja{{ parent() }}{% endblock %}

{% import 'components/forms.twig' as forms %}

{% block container %}
    <div class="row">
        <div id="box-register" class="col-sm-7">
            <h1>Darmowa rejestracja - stwórz swój profil</h1>

            {{ form_open({'method': 'POST', 'role': 'form', 'url': route('register'), 'class': 'form-horizontal'}) }}
                {{ forms.text('name', 'Nazwa użytkownika', input_old('name'), errors.first('name')) }}
                {{ forms.password('password', 'Hasło', input_old('password'), errors.first('password')) }}
                {{ forms.password('password_confirmation', 'Hasło (powtórnie)', input_old('password_confirmation'), errors.first('password_confirmation')) }}
                {{ forms.text('email', 'E-mail', input_old('email'), errors.first('email')) }}
                {{ forms.submit('Utwórz konto', 'Rejestracja...') }}
            {{ form_close() }}
        </div>

        <div id="box-register-sidebar" class="col-sm-5 hidden-xs">
            <div>
                <blockquote>Rejestracja zajmuje tylko kilka sekund i jest darmowa. Pozwoli Ci to aktywnie korzystać z naszego serwisu</blockquote>

                <h2>Korzyści wynikające z rejestracji konta</h2>

                <ul class="list-unstyled">
                    <li>Możliwość edycji oraz pisania artykułów</li>
                    <li>Obserwowanie wątków na forum</li>
                    <li>Śledzenie zmian w artykułach czy komentarzach</li>
                    <li>System szeregu powiadomień o aktywności na stronie</li>
                    <li>Pisanie własnego mikrobloga</li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}

{% block head %}
    {{ parent() }}

    <link rel="stylesheet" href="/css/auth.css">
{% endblock %}

{% block body %}
    {{ parent() }}

    <script type="text/javascript">
        $(function() {
            $('.form-horizontal').append('<input style="display: none" type="checkbox" name="human" value="1" checked="checked">');
        });
    </script>
{% endblock %}