{% macro confirm(message, title='Czy chcesz kontynuować?', id='confirm', okLabel='Tak, usuń') %}
    <div class="modal fade" id="{{ id }}" tabindex="-1" role="dialog" aria-labelledby="confirm-modal" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">{{ title }}</h4>
                </div>
                <div class="modal-body">
                    {{ message|raw }}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Anuluj</button>
                    <a href="javascript:" class="btn btn-danger danger">{{ okLabel }}</a>
                </div>
            </div>
        </div>
    </div>
{% endmacro %}

{% macro alert(message, title='Błąd', id='alert') %}
    <div class="modal fade" id="{{ id }}" tabindex="-1" role="dialog" aria-labelledby="alert-modal" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">{{ title }}</h4>
                </div>
                <div class="modal-body">
                    {{ message|raw }}
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">OK</button>
                </div>
            </div>
        </div>
    </div>
{% endmacro %}