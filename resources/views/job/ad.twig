<section class="box sidebar-job-offers {{ major_tag ? major_tag.name|replace({'.': '-', '#': 'sharp', 'c++': 'cpp'})|trim('.') }}">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4><a href="{{ route('job.home') }}" title="Szukaj pracy jako programista">Szukaj pracy {{ (major_tag ? (' w ' ~ (major_tag.getOriginal('logo') ? ('<img src="' ~ logo(major_tag.logo) ~ '">') : major_tag.real_name|default(major_tag.name))))|raw }}</a></h4>
        </div>
        <div class="panel-body">
            {% include 'components.jobs' %}

            <div class="media">
                <a href="{{ route('job.home') }}" class="text-align">Więcej ofert pracy</a>
            </div>
        </div>
    </div>
</section>
