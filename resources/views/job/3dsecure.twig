<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="{{ cdn('css/main.css') }}">

        <script>
            function onLoad() {
                document.form.submit();
            }
        </script>
    </head>
    <body onload="onLoad()">
        <div class="text-center padding-lg">
            <i class="fa fa-spinner fa-pulse fa-3x fa-fw"></i>
            <span class="sr-only">Loading...</span>
        </div>

        {{ form_open({url: url, name: 'form'}) }}
            {{ form_hidden('PaReq', data) }}
            {{ form_hidden('TermUrl', callback_url) }}
            {{ form_hidden('MD', uuid) }}
        {{ form_close() }}
    </body>
</html>
