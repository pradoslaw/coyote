<div id="js-comments" v-cloak>
  <h3>
    {{ icon('jobOfferComments') }}
    Pytania i odpowiedzi
  </h3>
  <vue-comment-form
      v-if="isAuthorized"
      resource-id="{{ job.id }}"
      resource="Coyote\Job"
  ></vue-comment-form>
  <div v-if="commentsCount > 0" class="card">
    <div class="card-body">
      <vue-comment
          v-for="comment in comments"
          :key="comment.id"
          :comment="comment"
      ></vue-comment>
    </div>
  </div>
  <vue-notifications></vue-notifications>
</div>
