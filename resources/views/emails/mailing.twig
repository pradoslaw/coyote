{% extends 'emails.layout' %}

{% block container %}
    {{ text|raw }}

    <p>Z poważaniem,<br>{{ config('app.name') }}</p>
{% endblock %}

{% block footnote %}
    <tr>
        <td class="footnote">
            Otrzymałeś tego e-maila ponieważ jesteście Państwo klientem {{ config('app.name') }}<br>
            <a href="{{ route('mailing.unsubscribe', [id]) }}">Kliknij tutaj</a> jeżeli nie chcesz więcej otrzymywać tego typu powiadomień.
        </td>
    </tr>
{% endblock %}
