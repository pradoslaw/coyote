<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=320, target-densitydpi=device-dpi">

    <style type="text/css">
        a, a:active, a:visited, a:hover {
            color: #80A41A;
            font-weight: bold;
            text-decoration: none;
        }

        hr {
            border-color: #ddd;
            border-style: dashed;
        }

        code {
            color: #c7254e;
            background-color: #f9f2f4;
            border-radius: 4px;
            padding: 1px 4px;
            font-size: 100%;
        }

        img {
            outline: none;
            text-decoration: none;
            display: block;
        }

        h1, h2, h3, h4, h5, h6 {
            line-height: 100% !important;
            -webkit-font-smoothing: antialiased;
            color: #80A41A;
        }

        h1 a, h2 a, h3 a, h4 a, h5 a, h6 a {
            color: inherit;
        }

        pre {
            display: block;
            padding: 8px;
            margin: 0 0 9px;
            font-size: 12px;
            line-height: 1.428571429;
            word-break: break-all;
            word-wrap: break-word;
            color: #333333;
            background-color: #f5f5f5;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        pre code {
            padding: 0;
            font-size: inherit;
            color: inherit;
            white-space: pre-wrap;
            background-color: transparent;
            border-radius: 0;
        }
    </style>
</head>
    <body style="background-color: #fafafa; margin: 0; padding: 0; font-family: Arial, 'Helvetica Neue', sans-serif;">
        <table cellpadding="0" cellspacing="0" style="max-width: 640px; width: 100%;  margin: 15px auto;">
            <tr>
                <td>
                    <table cellpadding="0" cellspacing="0" style="background-color: #fff; border: 1px solid #ddd; border-radius: 4px; padding: 0;">
                        <tr>
                            <td style="text-align: left; background: url({{ message.embed(public_path('img/background.png')) }}) repeat; border-bottom: 1px solid #d5d5d5;">
                                <a href="{{ route('home') }}">
                                    <img alt="4programmers.net" width="240" height="32" src="{{ message.embed(public_path('img/logo-email.png')) }}" border="0" align="top" style="display: inline; margin-top: 15px; margin-bottom: 15px; margin-left: 26px; ">
                                </a>
                            </td>
                        </tr>
                        <tr>
                            <td style="text-align: left; font-size: 14px; line-height: 18px; color: #444444; padding: 20px">
                                {% block container %}
                                    Witaj,<br><br>

                                    {% block content %}
                                    {% endblock %}

                                    <br><br>
                                    <b>Prosimy nie odpowiadać na tego e-maila. Jest on generowany automatycznie.</b>
                                {% endblock %}
                            </td>
                        </tr>
                    </table>
                </td>
            </tr>
            <tr>
                <td>
                    <a style="color: #777; text-decoration: none; font-size: 13px; margin-top: 10px; margin-left: 5px; font-weight: normal" href="{{ route('user.alerts.settings') }}">Ustawienia powiadomień</a>
                </td>
            </tr>
        </table>
    </body>
</html>
