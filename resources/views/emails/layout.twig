<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <meta name="viewport" content="width=320, target-densitydpi=device-dpi">

  <style type="text/css">
      body {
          background-color: #fafafa;
          margin: 0;
          padding: 0;
          font-family: Arial, 'Helvetica Neue', sans-serif;
          font-size: 14px !important;
      }

      .container {
          text-align: left;
          line-height: 1.5;
          color: #444444;
          padding: 20px;
          font-size: 14px;
      }

      a, a:active, a:visited, a:hover, .container a {
          color: #5e7813;
      }

      hr {
          border-color: #ddd;
          border-style: dashed;
      }

      code {
          color: #c7254e;
          background-color: #f9f2f4;
          border-radius: 4px;
          padding: 1px 4px;
          font-size: 100%;
      }

      img {
          outline: none;
          text-decoration: none;
          display: block;
      }

      ul, ol {
          padding-left: 16px;
      }

      .img-smile {
          display: inline;
          height: 1em;
      }

      h1, h2, h3, h4, h5, h6 {
          line-height: 100% !important;
          -webkit-font-smoothing: antialiased;
          color: #4d4d4d;
      }

      h1 a, h2 a, h3 a, h4 a, h5 a, h6 a {
          color: inherit;
      }

      .greeting {
          margin-top: 0;
          font-size: 19px;
          text-align: left;
      }

      pre {
          display: block;
          padding: 8px;
          margin: 0 0 9px;
          font-size: 12px;
          line-height: 1.428571429;
          word-break: break-all;
          word-wrap: break-word;
          color: #333333;
          background-color: #f5f5f5;
          border: 1px solid #ccc;
          border-radius: 4px;
      }

      pre code {
          padding: 0;
          font-size: inherit;
          color: inherit;
          white-space: pre-wrap;
          background-color: transparent;
          border-radius: 0;
      }

      blockquote {
          padding: 9px 18px;
          margin: 0 0 18px;
          font-size: 13px;
          border-left: 5px solid #eee;
          color: #777;
      }

      .action {
          width: 100%;
          margin: 30px auto;
          padding: 0;
          text-align: center;
      }

      .button {
          display: block;
          width: 200px;
          min-height: 20px;
          padding: 10px;
          background-color: #80a41a;
          border-color: #80a41a;
          border-radius: 4px;
          color: #ffffff;
          font-size: 15px;
          line-height: 25px;
          text-align: center;
          text-decoration: none;
          text-shadow: rgba(0, 0, 0, 0.14902) 0 -1px 0;
          box-shadow: inset 0 1px 1px #fff;
          -webkit-text-size-adjust: none;
      }

      .error {
          background-color: #d9534f;
      }

      .paragraph {
          font-size: 14px;
      }

      .paragraph-sub {
          margin-top: 0;
          color: #74787E;
          font-size: 12px;
          line-height: 1.5em;
      }

      .footnote {
          font-size: 11px;
          line-height: 1.8em;
          color: #74787E;
          padding-top: 10px;
          text-align: center;
      }

      .footnote a {
          color: inherit;
          text-decoration: underline;
      }
  </style>
</head>
<body>
<table cellpadding="0" cellspacing="0" style="max-width: 640px; width: 100%; margin-top: 15px; margin-bottom: 15px; margin-left: auto; margin-right: auto">
  <tr>
    <td>
      <table cellpadding="0" cellspacing="0" style="background-color: #fff; border: 1px solid #ddd; border-radius: 4px; width: 100%">
        <tr>
          <td style="text-align: left; background: url({{ cdn('img/background.png', true) }}) repeat; border-bottom: 1px solid #d5d5d5;">
            <a href="{{ route('home') }}">
              <img
                  src="{{ cdn('img/logo-email.png', true) }}?v1"
                  width="242" height="43"
                  alt="4programmers.net"
                  border="0"
                  align="top"
                  style="display:inline; margin-top:15px; margin-bottom:15px; margin-left:26px;">
            </a>
          </td>
        </tr>
        <tr>
          <td class="container">
            {% block container %}
              <h1 class="greeting">Witaj,</h1>
              {% block content %}
              {% endblock %}
            {% endblock %}

            {% block footer %}
            {% endblock %}
          </td>
        </tr>
      </table>
    </td>
  </tr>
  {% block footnote %}
    <tr>
      <td class="footnote">
        <a href="{{ route('user.notifications.settings') }}">
          Ustawienia powiadomień
        </a>
      </td>
    </tr>
  {% endblock %}
</table>
</body>
</html>
