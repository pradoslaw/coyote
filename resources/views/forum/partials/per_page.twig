<span class="form-inline">
    <select name="perPage" class="form-control input-sm" data-url="{{ url(request().path()) }}">
        {% for i in 1..5 %}
            <option value="{{ i * 10 }}" {{ perPage == i * 10 ? 'selected="selected"' }}>{{ i * 10 }}</option>
        {% endfor %}
    </select>
</span>