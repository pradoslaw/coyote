<div class="padding-sm-top">
  <ul class="tag-clouds cloud">
    {% for name, topics in tags %}
      <li>
        <a href="{{ route('forum.tag', [name|url_encode]) }}">{{ name }}</a><small>× {{ topics }}</small>
      </li>
    {% endfor %}
  </ul>
</div>
