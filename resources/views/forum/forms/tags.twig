{% extends 'forms.themes.horizontal.row' %}
{% set form_group_class = 'col-md-10' %}

{% import 'components.forms' as forms %}

{% block widget %}
    {# Nie moge tutaj uzyc funkcji form_text() poniewaz odczytuje ona wartosc pola z sesji #}
    {# a tutaj musimy zastosowac filtr join() i zamienic tablice na string #}
    <input type="text" name="{{ name }}" value="{{ children_values|join(', ') }}" {{ forms.attr(attr) }}>
{% endblock %}