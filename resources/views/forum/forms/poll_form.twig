<div role="tabpanel" class="tab-pane" id="poll">
    <div class="card card-forum">
        <div class="card-body">
            {% for field in children %}
                {{ form_row(field) }}
            {% endfor %}
        </div>
    </div>
</div>
