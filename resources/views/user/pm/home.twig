{% extends 'user/pm/base.twig' %}

{% block pm %}
    {{ pm.render() }}

    {% for item in pm %}
        {% include 'user/pm/_message.twig' with {'content': '<p class="pm-excerpt"><a href="' ~ route('user.pm.show', [item.id]) ~ '">' ~ excerpt(item.text, 255) ~ '</a></p>'} %}
    {% endfor %}

    {{ pm.render() }}
{% endblock %}