{% extends 'user.base' %}
{% block title %}Moje oceny{{ parent() }}{% endblock %}

{% block content %}
    <h2>Moje oceny</h2>

    <div id="box-start" class="card card-default">
        <div class="card-body">
            {{ grid(grid) }}
        </div>
    </div>
{% endblock %}
