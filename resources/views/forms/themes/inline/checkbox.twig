{% extends 'forms.themes.inline.row' %}

{% block label %}{% endblock %}
{% block wrap %}
    <div class="checkbox">
        {% if not ('[]' in name) %}
            {{ form_hidden(name, 0) }}
        {% endif %}

        {{ include(widget_path) }}

        <label for="{{ attr.id }}">
            {{ label }}
        </label>
    </div>
{% endblock %}